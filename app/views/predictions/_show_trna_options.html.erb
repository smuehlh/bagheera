<%= 
	content_tag(:p, "use blast low comple filter") +
	content_tag(:p, "create alignment with mafft") +
	content_tag(:p, "calc tree???")
	# show options: hat id "options_form", die disabled/ enabled wird; statt id class verwenden und hierfuer dieselbe class)
%>