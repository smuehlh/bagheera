<%=
content_tag(:div,
	file_field_tag(:uploaded_file,
	  	id: 	"ajax_upload_file",
	  	class: 	"ajax_upload_field",
		data: 	{url: post_upload_file_path}
	) +
	tag(:br) +
	content_tag(:small,
		"or ".html_safe +
		link_to("load example",
		  	get_upload_file_path( is_example: true ),
		    remote: true,
		    onclick: "hide_show_waiting('show');",
	    	style: "text-decoration: underline;",
	    	method: :get
    	),
		:id => "example_file_field"
	),
	:id => "file_upload_field"
) +
content_tag(:div,
	"No file uploaded",
	:id => "uploaded_files"
)
%>
